# TrustTag — Dependency Requirements

## Runtime (Server)
groq-sdk==1.1.1
express==4.18.3
cors==2.8.5
dotenv==16.4.5

## Dev (Server)
concurrently==8.2.2

## Runtime (Client)
react==18.x
react-dom==18.x
axios==1.x

## Dev (Client)
vite==5.x
@vitejs/plugin-react==4.x
tailwindcss==3.x
autoprefixer==10.x
postcss==8.x

---
Note: This project uses Node.js (npm) as its package manager, not pip.
Exact pinned versions are in:
  - /package.json          (server dependencies)
  - /client/package.json   (client dependencies)
  - /package-lock.json     (fully locked dependency tree)

Node.js >= 18.0.0 required.
